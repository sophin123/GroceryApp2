<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#000000"
    tools:context=".MainAcitivty.mainpage.mainpage.GroceryActivity.NoteActivity.Main2Activity">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:textSize="20dp"
        android:textColor="#E2DADA"
        android:textColorHint="#E2DADA"
        android:background="@drawable/edit_round"
        android:gravity="center"
        android:layout_margin="30dp"
        android:hint="@string/enter_text_here"
        android:id="@+id/noteEditText"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/add_activitymain2"
            android:textSize="18dp"
            android:textColor="#E2DADA"
            android:background="@drawable/additem_round"
            android:layout_margin="10dp"
            android:id="@+id/btnadd"/>

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/view"
            android:textSize="18dp"
            android:textColor="#E2DADA"
            android:background="@drawable/additem_round"
            android:layout_margin="10dp"
            android:id="@+id/btnview"/>
    </LinearLayout>
</LinearLayout>